#include "Clock.h"

Time getClockTime(void) {
    // Stub implementation returning a generic fixed date/time
    Time t = { .month = 1, .day = 1, .hour = 12, .minute = 0, .second = 0 };
    return t;
}
